<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">
    <!-- Notification Label -->
    <TextView
        android:id="@+id/notificationLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="Women's History Month"
        android:textSize="24sp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/description_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/notificationLabel"
        android:hint="The nationwide observance of Women History Month this year uses the theme Celebrating Women Who Tell Our Stories as a tribute to women in every community who have devoted their lives and talents to producing art and news, pursuing truth and reflecting society"
        android:inputType="textMultiLine"
        android:minLines="4"
        android:gravity="top|left" />

    <!--<Button
        android:id="@+id/button"
        android:layout_width="137dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/description_text"
        android:text="Back" />-->

    <!-- Image ImageView -->
    <ImageView
        android:id="@+id/backButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/description_text"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:src="@drawable/womens" />


</RelativeLayout>
